Welcome to Forge.

Forge helps keep your Obsidian vault clean, consistent, and dependable as it grows.

As vaults evolve over time, small inconsistencies start to accumulate:

- tags drift apart
- templates change
- fields get renamed
- dashboards stop matching notes
- Dataview queries become unreliable
- metadata becomes inconsistent
- old notes stop fitting newer workflows

Forge helps you repair and maintain that structure without forcing you into a rigid system.

---

![Forge Overview](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/forge-settings-overview.png?raw=true)

---

## What Forge Does

Forge provides maintenance and structure tooling for Obsidian vaults:

- Schema validation
- Vault linting
- Frontmatter normalization
- Tag normalization
- Bulk note update workflows
- Vault repair tools
- Shape validation
- Cached Forge health views for Obsidian Bases
- Relationship indexes
- Vault exports
- Maintenance workflows
- Vault-installed documentation
- Copy-ready examples

The goal is simple:

> Keep your vault usable and trustworthy over time.

---

## Your Installed Forge Locations

| Purpose | Path |
|---|---|
| Forge folder | `{{forge}}` |
| Docs | `{{docsFolder}}` |
| Examples | `{{examplesFolder}}` |
| Schema note | `{{schemaFile}}` |
| Patch note | `{{patchFile}}` |
| Patches folder | `{{patchesFolder}}` |
| Exports folder | `{{exportsFolder}}` |
| Inbox folder | `{{inboxFolder}}` |
| Shapes folder | `{{shapesFolder}}` |

---

## Recommended First Steps

### 1. Read the Quick Start

Start here:

[[getting-started/1. Quick Start|Quick Start]]

---

### 2. Install a Starter Schema

Copy the starter schema from [[{{examplesFolder}}/schema/1. Starter Schema|Starter Schema]] into:

```text
{{schemaFile}}
```

Schemas define the metadata rules and structure Forge uses throughout the vault.

---

### 3. Validate the Schema

Run:

```text
Forge: Validate Schema
```

This checks the schema itself for malformed YAML, invalid fields, and configuration issues.

---

![Schema Validation](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/schema-validation-errors.png?raw=true)

---

### 4. Run Your First Vault Lint

Run:

```text
Forge: Run Vault Lint
```

Forge scans your vault for structural inconsistencies such as:

- missing frontmatter
- malformed metadata
- inconsistent tags
- invalid field values
- stale review dates
- shape mismatches
- structural drift

---

![Vault Lint](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/vault-lint-overview.png?raw=true)

---

### 5. Fix a Small Batch of Findings

Use one of the repair workflows:

- Normalize tags
- Normalize frontmatter
- Apply a patch
- Run Vault Repair

Forge is designed for gradual cleanup.

You do not need to fix the entire vault at once.

---

### 6. Re-run Lint

After changes, run lint again.

That loop is Forge in miniature:

> define → validate → repair → repeat

---

## Core Workflows

### Schema Validation

[[reference/1. Schema Reference|Schema Reference]] defines the metadata contract for your vault.

Schemas help keep notes predictable so Dataviews, Bases, dashboards, and queries remain reliable.

---

![Schema Definition](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/schema-basic-definition.png?raw=true)

---

### Vault Linting

[[reference/2. Vault Lint|Vault Lint]] finds schema drift, malformed metadata, and inconsistent structures.

---

### Patch Engine

[[reference/3. Patch Engine|Patch Engine]] applies structured bulk changes using reviewable patch notes.

---

![Patch Engine](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/patch-engine-overview.png?raw=true)

---

### Vault Repair

[[workflows/1. Vault Repair|Vault Repair]] helps repair malformed frontmatter interactively.

---

![Vault Repair](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/frontmatter-repair-modal.png?raw=true)

---

### Normalize Tags and Frontmatter

[[workflows/2. Normalize Tags and Frontmatter|Normalize Tags and Frontmatter]] standardizes metadata formatting across the vault.

---

![Normalization](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/normalize-frontmatter-before-after.png?raw=true)

---

### Shapes

[[shapes/1. Shapes Overview|Shapes]] validates reusable note structures and hierarchies.

---

![Shape Validation](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/shape-lint-results.png?raw=true)

---

### Relationship Indexes and Exports

[[export/1. Export Overview|Export Overview]] generates inventory, relationship, and export files for dashboards and external tooling.

Forge calls these ontology indexes because they map relationships between notes and concepts across the vault.

You do not need to be an ontology expert to use them.

---

### Forge Health for Bases

[[workflows/4. Forge Health Bases View|Forge Health Bases View]] maps the files
already returned by a Base to Forge's cached lint, Shape, and review results.

The view is read-only while you filter or configure the Base. Use its explicit
**Refresh Forge health** action only when you want to run the normal Vault
Health refresh workflow.

Forge 3 requires Obsidian 1.10.0 or newer. Enable the Bases core plugin and run
**Reload plugins** after updating so Obsidian registers the layout.

---

![Relationship Indexes](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/ontology-index-export.png?raw=true)

---

### Settings and Utility Commands

- [[reference/6. Settings Reference|Settings Reference]] explains every settings tab and toggle.
- [[reference/7. Utility Commands|Utility Commands]] covers extra operational commands such as Rename Dataview Folder.

---

## What Forge Writes

Forge writes operational notes, backups, reports, patches, and exports inside your vault so they remain:

- searchable
- linkable
- syncable
- reviewable
- auditable

Forge stores plugin settings separately in Obsidian plugin data.

---

## Included Examples

Forge installs organized examples directly into your vault.

The examples are designed to be copied, modified, and adapted to your own workflows.

Useful starting points:

- [[{{examplesFolder}}/schema/1. Starter Schema|Starter Schema]]
- [[{{examplesFolder}}/workflows/1. First Lint Cleanup|First Lint Cleanup]]
- [[{{examplesFolder}}/patches/1. Basic Metadata Patch|Basic Metadata Patch]]

---

![Examples Folder](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/examples-folder-overview.png?raw=true)

---

## Recommended Mindset

Forge works best when treated as a maintenance workflow instead of a one-time cleanup tool.

You do not need:

- a perfectly structured vault
- complicated schemas
- enterprise workflows
- strict metadata everywhere

Start small.

Improve consistency gradually.

Let the vault evolve over time.
