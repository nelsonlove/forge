Use this when Forge has just been installed and you want a clean first pass without trying to reorganize your entire vault in one afternoon.

Forge works best as a gradual maintenance workflow.

You do not need:

- a perfect schema
- strict metadata everywhere
- a fully organized vault
- complicated automation

Start small.

Improve consistency over time.

---

![Install Documentation](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/install-docs-command.png?raw=true)

---

# 1. Install the Documentation

Run:

```text
Forge: Install Documentation
```

Forge installs documentation into:

```text
{{docsFolder}}
```

Examples install into:

```text
{{examplesFolder}}
```

The installer skips files that already exist.

If you want a fresh copy later, move or rename the previous installed docs first.

---

# 2. Explore the Installed Structure

Forge creates an operational workspace directly inside your vault.

Typical structure:

```text
{{forge}}/
├── Docs/
├── Examples/
├── Patches/
├── Schemas/
├── Shapes/
└── Exports/
```

---

![Installed Docs](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/installed-docs-tree.png?raw=true)

---

# 3. Create Your Schema Note

Forge expects the schema at:

```text
{{schemaFile}}
```

Copy the starter schema from [[{{examplesFolder}}/schema/1. Starter Schema|Starter Schema]].

Schemas define the metadata rules and structure Forge uses throughout the vault.

A schema note needs:

- normal frontmatter
- a body line like:
  ```text
  version:: "1.0"
  ```
- a `# Contract` heading
- one fenced YAML block under that heading

---

![Schema Definition](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/schema-basic-definition.png?raw=true)

---

# 4. Validate the Schema

Run:

```text
Forge: Validate Schema
```

This checks the schema itself for:

- malformed YAML
- invalid field definitions
- missing configuration
- invalid enum rules
- duplicate fields

Fix schema problems before linting the vault.

Vault linting depends on a readable schema.

---

![Schema Validation](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/schema-validation-errors.png?raw=true)

---

# 5. Run Your First Vault Lint

Run:

```text
Forge: Run Vault Lint
```

Forge scans your vault for issues such as:

- missing frontmatter
- inconsistent tags
- malformed metadata
- invalid field values
- stale review dates
- shape mismatches
- structural drift

Forge writes lint reports into:

```text
{{exportsFolder}}
```

---

![Vault Lint](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/vault-lint-overview.png?raw=true)

---

# 6. Fix One Small Thing

Do not try to clean the entire vault at once.

Pick one narrow improvement, such as:

- add a missing `status`
- normalize tags in one folder
- repair malformed frontmatter
- standardize frontmatter ordering
- clean up one note type

Good early repair tools:

- `Forge: Normalize Tags`
- `Forge: Normalize Frontmatter`
- `Forge: Vault Repair`

Useful repair examples:

- [[{{examplesFolder}}/patches/1. Basic Metadata Patch|Basic Metadata Patch]]
- [[{{examplesFolder}}/patches/2. Tag Cleanup Patch|Tag Cleanup Patch]]
- [[{{examplesFolder}}/workflows/1. First Lint Cleanup|First Lint Cleanup]]

---

![Normalization](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/normalize-frontmatter-before-after.png?raw=true)

---

# 7. Re-run Lint

After repairs, run lint again.

That feedback loop is the core Forge workflow:

```text
schema → lint → review → repair → lint again
```

Over time, the vault becomes easier to:

- search
- query
- maintain
- export
- build dashboards from
- use with Dataview
- use with Bases
- use with AI workflows

---

# 8. Explore the Examples

Forge installs organized examples directly into your vault.

Examples include:

- starter schemas
- patch workflows
- repair examples
- export workflows
- shape systems
- maintenance routines

The examples are designed to be copied and adapted to your own workflows.

Good places to start:

- [[{{examplesFolder}}/schema/1. Starter Schema|Starter Schema]]
- [[{{examplesFolder}}/patches/1. Basic Metadata Patch|Basic Metadata Patch]]
- [[{{examplesFolder}}/workflows/2. Monthly Maintenance|Monthly Maintenance]]

---

![Examples Folder](https://github.com/joshua-walls/forge/blob/main/assets/screenshots/examples-folder-overview.png?raw=true)

---

# 9. Explore Forge Health in Bases

On Obsidian 1.10.0 or newer, enable the Bases core plugin and run **Reload
plugins**. Open or create a Base, then choose the **Forge health** layout to see
cached lint, Shape, and review status for the current Base result set.

Run **Refresh Forge health** once after upgrading to Forge 3 so clean and
not-scanned notes can be distinguished. Filtering or configuring the Base stays
read-only and does not start a vault scan.

See [[../workflows/4. Forge Health Bases View|Forge Health Bases View]] for the
full workflow and view options.

---

# Recommended Next Reads

- [[../reference/1. Schema Reference|Schema Reference]]
- [[../reference/2. Vault Lint|Vault Lint]]
- [[../reference/3. Patch Engine|Patch Engine]]
- [[../workflows/1. Vault Repair|Vault Repair]]
- [[../workflows/4. Forge Health Bases View|Forge Health Bases View]]
- [[../shapes/1. Shapes Overview|Shapes Overview]]
- [[../export/1. Export Overview|Export Overview]]
